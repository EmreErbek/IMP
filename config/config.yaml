# Investment Management Panel Configuration

# Data Settings
data:
  default_period: "1y"  # Options: 1d, 5d, 1mo, 3mo, 6mo, 1y, 2y, 5y, 10y, ytd, max
  default_interval: "1d"  # Options: 1m, 2m, 5m, 15m, 30m, 60m, 90m, 1h, 1d, 5d, 1wk, 1mo, 3mo
  market_suffix: ".IS"  # Borsa Istanbul suffix
  cache_enabled: true
  cache_duration_hours: 24

# Database Settings
database:
  type: "sqlite"
  path: "data/imp.db"
  echo: false  # Set to true for SQL debug logs

# Technical Indicators Default Settings
indicators:
  rsi:
    period: 14
  macd:
    fast_period: 12
    slow_period: 26
    signal_period: 9
  bollinger_bands:
    period: 20
    std_dev: 2
  moving_average:
    short_period: 20
    medium_period: 50
    long_period: 200

# Logging
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  file_path: "data/imp.log"
  max_bytes: 10485760  # 10MB
  backup_count: 5

# Portfolio Settings
portfolio:
  default_currency: "TRY"
  commission_rate: 0.001  # 0.1% commission

# Alert Settings
alerts:
  enabled: false  # Enable alert system
  check_interval_minutes: 15  # How often to check alerts
  email:
    smtp_host: "smtp.gmail.com"
    smtp_port: 587
    sender_email: ""  # Your email address
    sender_password: ""  # Your app password (for Gmail, use App Password, not your regular password)

# Web Dashboard Settings
web:
  host: "localhost"
  port: 8501
  theme: "light"  # light or dark
